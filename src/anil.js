function formatName(user) {
    return user.firstName + ' ' + user.lastName;
  }
  
  const user = {
    firstName: 'Anil',
    lastName: 'Banala'
  };
  
  const Element = (
    <h1>
      Hello,!
    </h1>
  );
  export default Element;